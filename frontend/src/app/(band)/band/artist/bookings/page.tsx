"use client";

import { BookingWorkspace } from "@/components/bookings/workspace/BookingWorkspace";

export default function ArtistBookingsWorkspacePage() {
  return <BookingWorkspace role="artist" />;
}
